import Layer from "../components/Layer";
import TodayPlayers from "../features/TodayPlayers";

const Root = () => {
  return (
    <Layer>
      <TodayPlayers />
    </Layer>
  );
};

export default Root;
